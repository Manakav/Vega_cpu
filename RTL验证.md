# RTL 待验证清单

说明：本文档仅用于整理当前 CPU 设计的待验证项，不代表任何条目已经完成验证。

## 1. 模块级待验证

### 1.1 IF 相关模块

- [ ] if_stage：双路取指、PC 推进、valid 生成、预测命中时 Way2 失效
- [ ] btb：复位状态、索引/标记匹配、命中返回目标地址、更新替换策略
- [ ] bht：复位默认值、2-bit 饱和计数器更新、GHR 异或索引、taken 输出
- [ ] icache：命中读、未命中填充、组相联替换、字节/字对齐访问

### 1.2 ID 与寄存器堆相关模块

- [ ] decoder：RV64I 基础指令译码、M 扩展译码、C 扩展译码、非法指令处理
- [ ] id_stage：双路译码一致性、IDII 锁存、stall 保持、flush 注气泡
- [ ] register_file：4 读口、2 写口、同周期读写旁路、x0 恒为 0、Way2 写优先级

### 1.3 II / 冒险处理相关模块

- [ ] ii_stage：双发射仲裁、Way1 单发射降级、Way2 重试、IIEX 锁存正确性
- [ ] hazard_unit：load-use 冒险暂停、分支误预测冲刷、双发射停顿联动

### 1.4 EX 相关模块

- [x] alu：算术、逻辑、比较、移位、标志位输出、默认分支行为
- [ ] mul_div：MUL/MULH/MULHSU/MULHU、DIV/DIVU、REM/REMU 边界情况
- [ ] ex_stage：双路 ALU 并行执行、前递优先级、分支判断、mispredict 反馈

### 1.5 MEM / CSR 相关模块

- [ ] mem_stage：Way1 访存、Way2 透传、load 数据扩展、store 字节使能
- [ ] dcache：命中/未命中、写回或直写策略、访存宽度支持、替换策略
- [ ] csr_unit：CSR 读写、异常/中断入口、返回路径、关键 CSR 状态更新

## 2. 流水线级待验证

### 2.1 IF 级

- [ ] PC 初值、复位后行为、连续取指推进
- [ ] 压缩指令与 32 位指令混排时的长度判断
- [ ] 分支预测命中、未命中、误预测恢复路径

### 2.2 ID 级

- [ ] 双路输入同时译码
- [ ] 控制信号在 stall 时保持稳定
- [ ] flush 后无脏控制信号残留

### 2.3 II 级

- [ ] RAW 冒险阻断双发射
- [ ] WAW 冒险阻断双发射
- [ ] 双访存结构冒险阻断双发射
- [ ] 分支或跳转指令独占发射
- [ ] CSR/系统指令阻断双发射

### 2.4 EX 级

- [ ] EX/MEM 与 MEM/WB 双路前递覆盖 rs1/rs2 所有组合
- [ ] 分支条件判断准确
- [ ] JAL/JALR 目标地址计算正确
- [ ] M 扩展结果与 ALU 路径选择正确

### 2.5 MEM/WB 级

- [ ] load 数据回写路径正确
- [ ] 双路写回目的寄存器和写使能正确
- [ ] 双路同周期写回冲突处理符合设计预期

## 3. 系统级待验证

### 3.1 顶层接口

- [ ] 指令口握手时序正确
- [ ] 数据口读写请求、写使能、字节使能正确
- [ ] 中断输入与调试接口不会破坏正常流水线控制

### 3.2 顶层 CPU 集成

- [ ] riscv_cpu 顶层六级流水线联通正确
- [ ] stall_if、stall_id、flush_if、flush_id、flush_ex 优先级正确
- [ ] 分支误预测时 IF/ID/II 各级状态恢复正确
- [ ] 单发射退化路径与双发射路径结果一致

### 3.3 指令子集功能

- [ ] RV64I 常用算术逻辑指令
- [ ] Load/Store 指令
- [ ] Branch/Jump 指令
- [ ] AUIPC/LUI 指令
- [ ] M 扩展指令
- [ ] C 扩展常用压缩指令
- [ ] CSR/异常/中断相关指令

## 4. 场景级待验证

### 4.1 正常执行场景

- [ ] 连续独立整数指令双发射
- [ ] 一路算术一路访存混合执行
- [ ] 压缩指令与标准指令交替执行

### 4.2 冒险场景

- [ ] Way2 读取 Way1 刚写寄存器
- [ ] 两路写同一目的寄存器
- [ ] load-use 紧邻相关
- [ ] 前递可解与不可解情况区分正确

### 4.3 控制流场景

- [ ] BTB 命中且预测 taken
- [ ] BTB 未命中
- [ ] BHT 方向预测正确与错误
- [ ] 误预测后刷流水线并从正确 PC 恢复

### 4.4 访存场景

- [ ] Byte/Half/Word/Double 读写
- [ ] 有符号与无符号 load 扩展
- [ ] 非对齐访问响应符合设计预期
- [ ] 单端口数据存储器下双发射约束有效

### 4.5 异常与系统场景

- [ ] 非法指令异常
- [ ] ECALL/EBREAK
- [ ] 定时器中断、外部中断、软件中断
- [ ] 中断返回后流水线恢复
